trigger:
  branches:
    include:
    - main

pool:
  vmImage: 'ubuntu-latest'

variables:
  application: examples/TemperatureController

steps:
- template: steps/build.yaml
- template: steps/publish.yaml
